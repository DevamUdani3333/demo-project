version: '3.8'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "5000:80"

  frontend:
    build:
      context: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
